from __future__ import annotations

from datetime import datetime


def rebuild_sitemap() -> str:
    # placeholder for actual sitemap generation
    return f"sitemap rebuilt at {datetime.utcnow().isoformat()}"
